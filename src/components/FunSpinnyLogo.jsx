import React from 'react';
import logo from '../logo.svg';

const FunSpinnyLogo = () =>{
    return (
        <div class="flex justify-center items-center h-screen">
            <img src={logo} className="App-logo" alt="logo" />
        </div>
    );
};

export default FunSpinnyLogo;